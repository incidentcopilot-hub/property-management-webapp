{
  "name": "property-management-webapp-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "bash -c 'trap \"kill 0\" EXIT; npm --prefix backend run dev & npm --prefix frontend run dev; wait'"
  }
}
